cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(light_wifi_sensor)

#Đây là CMakeLists.txt gốc, chỉ định phiên
#bản CMake, load hệ thống ESP-IDF và đặt tên project.